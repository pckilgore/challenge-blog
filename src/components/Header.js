import React from 'react'
import logo from '../logo.svg'

const Header = props => (
  <header>
    <img src={logo} className="logo" alt="logo" />
    <h1 className="title">Patrick's Blog</h1>
  </header>
)

export default Header
